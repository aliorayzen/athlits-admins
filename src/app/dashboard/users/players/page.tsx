import { PlayersDirectory } from "./_components/players-directory";

/** Player reporting directory with server filters and grouped booking history. */
export default function PlayersPage() {
  return <PlayersDirectory />;
}
